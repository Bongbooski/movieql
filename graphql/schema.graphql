type Query {
  movies: [Movie]!
  movie(id: Int!): Movie
}

type Movie {
  name: String!
  score: Int!
  id: Int!
}

type Mutation {
  addMovie(name: String!, score: Int!): Movie!
  deleteMovie(id: Int!): Boolean
}
